var buttons = document.getElementById('button-row');
buttons.style.display = "";

var form = document.getElementById('new-extension-form');
form.remove();

var header = document.getElementById('table-header');
var new_content = '<%= escape_javascript(render @extension) %>';
header.insertAdjacentHTML('afterend', new_content);