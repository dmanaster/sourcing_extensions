var buttons = document.getElementById('button-row');
var table = document.getElementById('table-row');
buttons.style.display = "none";
table.insertAdjacentHTML('beforebegin', '<%= escape_javascript(render "form", :extension => @extension) %>');
