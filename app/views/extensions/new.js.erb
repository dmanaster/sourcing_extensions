var new_link = document.getElementById('new-link');
var div = document.getElementById('new-extension');
new_link.style.display = "none";
div.insertAdjacentHTML('beforeend', '<%= escape_javascript(render "form", :extension => @extension) %>');
