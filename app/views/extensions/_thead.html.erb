<thead id = "extension-head-<%= extension.id %>" class="thead-dark extension-<%= extension.id %>">
  <tr>
    <th id = "extension-name-<%= extension.id %>"><%= link_to extension.name, extension.url, target: :_blank %></th>
    <th></th>
    <th></th>
    <th class="text-right">
      <%= link_to '<i class="far fa-edit"></i>'.html_safe, edit_extension_path(extension), remote: true, class: "mx-2" %>
      <%= link_to '<i class="far fa-trash-alt"></i>'.html_safe, extension, method: :delete, remote: true,class: "mx-2", data: { confirm: 'WARNING: This will delete this extension and all tracked versions. Are you sure?' } %>
    </th>
  </tr>
</thead>
